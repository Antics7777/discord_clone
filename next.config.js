/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "https://uploadthing.com",
            "utfs.io"
        ]
    }
}

module.exports = nextConfig
